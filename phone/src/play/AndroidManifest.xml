<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Wearable Data Layer favorites-sync listener. Play edition only; the manifest
             merger unions this into the play variant. The fdroid variant has no counterpart
             (no GMS, no watch sync). -->
        <service
            android:name=".PhoneFavoritesSyncListener"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/favorites/v2" />
            </intent-filter>
        </service>
    </application>
</manifest>
